---
title: "Embedding Shiny in Quarto"
format: html
---

# Introduction

This is an example of embedding a Shiny app within a Quarto document.

```{r}
#| echo: false
#| include: false
library(shiny)
library(quarto)

#| standalone: true
shinyApp(
  ui = fluidPage(
    titlePanel("Hello Shiny!"),
    sidebarLayout(
      sidebarPanel(
        sliderInput("obs", "Number of observations:",
                    min = 1, max = 1000, value = 500)
      ),
      mainPanel(
        plotOutput("distPlot")
      )
    )
  ),
  server = function(input, output) {
    output$distPlot <- renderPlot({
      hist(rnorm(input$obs))
    })
  }
)


### Key Points

1. **Document Header**: Ensure the `format` is set to `html` to enable Shiny.
2. **Hidden Library Load**: Load the necessary libraries (like `shiny`) in a hidden code chunk.
3. **Shiny App**: Define the Shiny app within a code chunk and set `standalone: true`.

### Running the Document

To render the document, use the following command in your terminal:

```sh
quarto render your_document.qmd
