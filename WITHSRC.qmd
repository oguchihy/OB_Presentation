---
title: "Embedded Shiny App"
format: html
server: shiny
---

```{r setup, include=FALSE}
# Ensure necessary libraries are loaded
library(shiny)
library(survival)
library(survminer)
library(ggplot2)
library(myRFunctions)  # This includes ggforest3
library(nnet)  # for multinom()
library(dplyr)
library(randomForest)
```

```{r}
source(file = "C:/Users/onkwocha/Dropbox/OB_DATA/gd_qto_shny.R")
```

```{r}
shinyApp(ui = ui, server = server)
```

